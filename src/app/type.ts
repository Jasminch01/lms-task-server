export type Tcourse = {
  title: String;
  price: String;
  thumbnail: String;
  description: String;
};

export type Tmodule = {
  coruseId: string;
  title: string;
  moduleNumber: Number;
};

export type Tadmin = {
  name: string;
  email: string;
  password: string;
};
